# 투자 리서치 시스템 — 운영 규칙

> 매매는 사람이 한다. 이 시스템은 리서치·정리·계산·모니터링만 한다.

이 문서는 **모든 스킬에 예외 없이 적용된다.** 개별 `SKILL.md`에 이 내용을 다시 쓰지 말고,
그 스킬만의 절차와 출력 형식만 기술한다.

## 1. 절대 규칙 5가지

### 출처 규칙
- 출처 없는 숫자는 산출물에 쓰지 않는다. 모르면 `확인 필요`로 표기한다.
- 모든 수치는 `src/provenance.py`의 `Sourced[T]`로 감싸여야 한다. 맨 숫자를 렌더에 넘기면
  `require_sourced()`가 예외를 던진다. 이건 권고가 아니라 실행되는 제약이다.
- **로컬 문서**(업로드한 10-K 등)는 페이지까지 인용한다.
- **웹/API 출처는 URL만** 인용한다. 페이지 번호를 붙이지 않는다 — 가장 위험한 환각 지점이다.
  `Locator`는 이 규칙을 타입 단계에서 강제한다.

### 계산 규칙
- 모든 수치 계산은 `src/core/` 의 함수로 실행한다. 대화 중 암산 금지.
- 역DCF는 `core/valuation/reverse_dcf.py`의 이분법으로만 구한다.
- 이상치(FCF가 3년 평균 대비 ±40% 이탈 등)는 `core/valuation/outliers.py`로 자동 감지해 보고한다.

### 사실 / 해석 분리
- 자료에 있는 것은 `[사실]`, 판단은 `[해석]`으로 표기한다.
- **변화가 없으면 "변화 없음"이 정답이다.** 억지로 스토리를 만들지 않는다.

### 매매 신호 금지
- "사라 / 팔아라"를 말하지 않는다. **"어디를 더 파봐야 하는지"만** 제시한다.
- 리밸런싱·세금은 계산과 시나리오만 보여주고 비중을 정해주지 않는다.
- 목표주가·적정주가를 산출하지 않는다. 역DCF는 "이 가격이 요구하는 성장률"만 제시한다.

### AI는 1차 스크리너
- 오류가 0%가 아니라는 전제로 동작한다. 후보를 좁히는 도구로만 쓴다.
- 투자 판단에 직접 쓰는 숫자는 원문에서 재확인하도록 산출물에 명시한다.

## 2. 출처 등급

| 등급 | 정의 | 예 | 수치 사용 |
|------|------|-----|-----------|
| `PRIMARY` (1차) | 발행 주체가 직접 배포 | SEC EDGAR, OpenDART, FRED, ECOS, ECB, ETF 발행사 | 허용 |
| `VENDOR` (2차) | 벤더가 정규화·집계 | FMP, Alpha Vantage | 허용 (벤더명 표기) |
| `SCRAPED` (3차) | 비공식 스크래핑·웹검색 | yfinance, 웹검색 | **수치 금지. 정성 전용** |

3차 출처로 수치 사실을 만들려 하면 `provenance.py`가 거부한다.
웹검색은 뉴스·테마·시나리오 등 정성 분석에만 쓴다.

## 3. 계층과 의존 방향

```
models  ←  sources  (외부 I/O. 벤더가 바뀌면 여기만 바뀐다)
   ↑
   ├─────  core     (순수 계산. I/O 금지. 네트워크 없이 테스트된다)
   ↑
   └─────  render   (표현)
```

**하드 룰: 의존은 항상 안쪽으로만.**
`core/`는 `sources/`를 import하지 않는다. 계산 함수는 값을 받아 값을 반환한다.
`tests/test_layering.py`가 이걸 검사한다.

## 4. 폴더 역할

| 경로 | 역할 | 쓰는 주체 | 지우면 |
|------|------|-----------|--------|
| `CLAUDE.md` | 이 헌법 | 사람 | 복구 불가 |
| `.claude/skills/` | 트리거 문구 + 절차 + 출력 형식 | 사람 | 복구 불가 |
| `src/models.py` | 도메인 어휘 (자산유형·보유·공시) | 사람 | 복구 불가 |
| `src/provenance.py` | 출처 규칙의 구현체 | 사람 | 복구 불가 |
| `src/sources/` | 외부 데이터 경계. 출처 1개 = 파일 1개 | 사람 | 복구 불가 |
| `src/core/` | 순수 계산·문구 비교 | 사람 | 복구 불가 |
| `src/render/` | md·html 렌더 | 사람 | 복구 불가 |
| `portfolio/` | **수동 갱신. 유일한 진실의 원천** | 사람 | **복구 불가** |
| `ledger/manifest.jsonl` | 무엇을·언제·어디서 받았나 (append-only) | 기계 | 복구 불가 |
| `data/` | 캐시·원문. **통째로 지워도 재생성됨** | 기계 | 재생성 |
| `reports/` | 날짜별 산출물 | 기계 | 시점 기록 소실 |
| `dashboard/` | 단일 현재 상태 HTML | 기계 | 갱신 1회로 복구 |

## 5. 운영 원칙

- **브로커 연동은 시세·종목 정보 조회로만 제한한다.** (2026-08-24 개정)
  증권사 API를 전면 금지했으나, 시세는 보유 종목 전체에 대해 매일 필요한 고빈도 데이터여서
  수동 입력이 성립하지 않는다. 대신 다음 경계를 코드로 강제한다:
  - `sources/toss.py`는 **GET만** 보내고, 허용 경로 화이트리스트 밖은 호출 자체가 예외다.
  - **`X-Tossinvest-Account` 헤더를 생성하는 코드를 두지 않는다.** 이 헤더 없이는
    계좌·자산·주문 API가 동작하지 않으므로 주문 경로가 구조적으로 존재하지 않게 된다.
  - 허용 IP 등록을 필수로 한다. 유출된 자격증명도 미등록 IP에서는 403이다.
  - `tests/test_layering.py`가 주문 경로 부재를 검사한다.
- **보유 현황은 계속 수동이다.** `/api/v1/holdings`는 계좌 헤더가 필요하므로 쓰지 않는다.
  원칙: **저빈도 데이터는 수동, 고빈도 데이터는 자동.** 보유 종목 구성은 몇 주에 한 번 바뀌고
  이 시스템의 유일한 진실의 원천이므로 사람이 `portfolio/holdings.yaml`을 갱신한다.
- **자동 스케줄 실행을 하지 않는다.** 호출할 때만 돈다. 별도 API 청구를 피하고
  구독 한도 안에서만 쓴다.
- **한국 상장사 한계를 명시한다.** 어닝콜 전문 공개가 미국만큼 흔치 않으므로,
  스토리 분석의 정밀도가 낮다는 사실을 산출물에 스스로 적는다.
- 트랜스크립트가 없어서 분석하지 못한 항목은 조용히 웹검색으로 대체하지 않고
  `확인 필요 (트랜스크립트 미확보)`로 남긴다.

## 6. 자주 쓰는 명령

```bash
python3 -m pytest tests/ -q          # 원칙이 코드로 지켜지는지 검사
python3 -m src.sources.sec_edgar AAPL   # 1차 출처 단독 확인
```
